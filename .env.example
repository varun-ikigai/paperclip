DATABASE_URL=postgres://paperclip:paperclip@localhost:5432/paperclip
PORT=3100
SERVE_UI=false

# OpenRouter API key — required for OpenCode agents using ZDR models via OpenRouter.
# Each agent can also have its own key set in adapterConfig.env.
# OPENROUTER_API_KEY=sk-or-...
